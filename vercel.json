{
    "buildCommand": "npm install --legacy-peer-deps && CI=false NODE_OPTIONS=--openssl-legacy-provider npm run build",
    "installCommand": "npm install --legacy-peer-deps",
    "framework": "create-react-app"
}